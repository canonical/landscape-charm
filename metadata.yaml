name: landscape-appserver
summary: The Landscape application service charm.
description: |
  A multi server Landscape charm.
requires:
  db-admin:
    interface: pgsql
  db-admin-proxy:
    interface: pgsql
  amqp:
    interface: rabbitmq
  landscape-appserver:
    interface: landscape
