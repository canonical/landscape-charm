name: landscape-msgserver
summary: The Landscape message service charm.
description: |
  A multi server Landscape charm.
requires:
  db-admin:
    interface: pgsql
  db-admin-proxy:
    interface: pgsql
  amqp:
    interface: rabbitmq
