#!/usr/bin/python
import sys

from lib import setup

setup.pre_install()

from lib.install import InstallHook


if __name__ == "__main__":
    install_hook = InstallHook()
    sys.exit(install_hook.run())
